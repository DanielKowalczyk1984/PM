get_filename_component(ORUTILS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${ORUTILS_CMAKE_DIR})
list(REMOVE_AT CMAKE_MODULE_PATH -1)

if(NOT TARGET ORUtils::ORUtils)
    include("${ORUTILS_CMAKE_DIR}/ORUtilsTargets.cmake")
endif()

set(ORUTILS_lIBRARIES ORUtils::ORUtils)